apiVersion: v1
kind: Service
metadata:
  name: chmod
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "chmod"
    port: 8080
  selector:
    app: chmod
